<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ivenavm.theskinny">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/skinney"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheSkinny"
        android:usesCleartextTraffic="true">
        <activity android:name=".DataitemActivity"></activity>
        <activity android:name=".AddActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".NpureActivity" />
        <activity android:name=".AvoskinActivity" />
        <activity android:name=".PyunkangActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".DataActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>